import {Expansion} from "./expansion";
import {MarketCardType} from "./martet-card-type";

export class MarketCard {
    name: string;
    type: MarketCardType;
    cost: number;
    expansion: Expansion;
}
